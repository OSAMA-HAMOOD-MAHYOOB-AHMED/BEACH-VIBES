# Copy this file to .env.local and fill in the backend API's URL.
# The frontend never talks to Supabase directly — everything goes through
# backend/ (see backend/.env.example for the Supabase/JWT setup there).

VITE_API_URL=http://localhost:4000
